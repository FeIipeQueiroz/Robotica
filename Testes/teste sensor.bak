task main()
{
  SetSensorLight(IN_2, true);
  SetSensorTouch(IN_1);
  SetSensorUltrasonic(IN_4);
  
  /*while(SensorUS(IN_4) < 30)
  {
    TextOut(0, LCD_LINE3, "ultrassonico: " + NumToStr(SensorUS(IN_4)), true);
    //OnRevSync(OUT_AC, 30, 100);
  }   */
  //Wait(500);
  //Off(OUT_AC);
  
  while(true){
  //OnRevSync(OUT_AC, 30, 0);
  TextOut(0, LCD_LINE3, "Luz: " + NumToStr(Sensor(IN_2)), true);
   if(Sensor(IN_2) < 68 && Sensor(IN_2) > 65)
   {
     //Off(OUT_AC);
     Wait(500);
     SetSensorLight(IN_2, false);
     Wait(500);

     if((Sensor(IN_2) < 24 && Sensor(IN_2) > 15))
     {
          TextOut(0, LCD_LINE3, "Luz: " + NumToStr(Sensor(IN_2)), true);
        //TextOut(0, LCD_LINE3, "Vermelho!!!", true);
        //OnFwdSync(OUT_AC, 30, 0);
        SetSensorLight(IN_2, true);
        Wait(500);
     } else {
        TextOut(0, LCD_LINE3, "Luz: " + NumToStr(Sensor(IN_2)), true);
        //TextOut(0, LCD_LINE3, "Era Fic", true);
        SetSensorLight(IN_2, true);
        //    OnRevSync(OUT_AC, 30, 0);
        Wait(300);
     }
   }
  }

  //OnRevSync(OUT_AC, 75, 0);
 /* while(true){
   if(30 > SensorUS(IN_4)){

     OnRevSync(OUT_AC, 50, 100);
     Wait
   }
  }    */
 }
